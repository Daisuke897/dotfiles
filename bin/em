#!/usr/bin/env bash

case "$OSTYPE" in
    linux-gnu*)
        export SHELL=/bin/bash
        ;;
    darwin*)
        export SHELL=/bin/zsh
        ;;
    *)
        export SHELL=/bin/sh
        ;;
esac

export TERM=xterm-256color

exec direnv exec ~/dotfiles emacs "$@"
